export interface StatusFile {
  validated: string;
  valid: string;
  rejected: string;
  invalid: string;
  carried: string;
  tracked: string;
  registered: string;
  collidence: string;
  considered: string;
}